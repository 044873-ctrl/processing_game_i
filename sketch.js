let player;let bullets=[];let enemies=[];let particles=[];let stars=[];let score=0;let gameOver=false;function setup(){createCanvas(400,600);player={x:width/2,y:height-30,r:16,speed:5};for(let i=0;i<30;i++){stars.push({x:random(0,width),y:random(0,height),s:random(0.5,2)})}textAlign(LEFT,TOP);textSize(16)}function draw(){background(0);noStroke();fill(255);for(let i=0;i<stars.length;i++){let s=stars[i];s.y+=s.s;if(s.y>height){s.y=0;s.x=random(0,width)}ellipse(s.x,s.y,2,2)}if(!gameOver){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed}if(player.x<player.r){player.x=player.r}if(player.x>width-player.r){player.x=width-player.r}}fill(0,200,255);ellipse(player.x,player.y,player.r*2,player.r*2);for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y+=b.vy;if(b.y<-b.r){bullets.splice(i,1);continue}fill(255,255,0);ellipse(b.x,b.y,b.r*2,b.r*2)}if(!gameOver&&frameCount%60===0){enemies.push({x:random(12,width-12),y:-12,r:12,vy:2})}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];e.y+=e.vy;fill(255,0,0);ellipse(e.x,e.y,e.r*2,e.r*2);if(dist(e.x,e.y,player.x,player.y)<=e.r+player.r){gameOver=true}if(e.y>height+e.r){enemies.splice(i,1);continue}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];if(dist(e.x,e.y,b.x,b.y)<=e.r+b.r){for(let k=0;k<5;k++){let angle=random(0,TWO_PI);let speed=random(1,3);particles.push({x:e.x,y:e.y,vx:cos(angle)*speed,vy:sin(angle)*speed,life:20,r:3})}enemies.splice(i,1);bullets.splice(j,1);score++;break}}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life--;fill(255,150,0);ellipse(p.x,p.y,p.r*2,p.r*2);if(p.life<=0){particles.splice(i,1)}}fill(255);text('Score: '+score,10,10);if(gameOver){textAlign(CENTER,CENTER);text('GAME OVER',width/2,height/2);textAlign(LEFT,TOP)}}function keyPressed(){if(!gameOver&&keyCode===32){bullets.push({x:player.x,y:player.y-player.r-4,r:4,vy:-8})}}
