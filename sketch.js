let playerX,playerY,playerSpeed,playerRadius;let bullets=[];let enemies=[];let particles=[];let stars=[];let score=0;let gameOver=false;let lastShotFrame=-999;let shotCooldown=8;function setup(){createCanvas(400,600);playerX=width/2;playerY=height-40;playerSpeed=5;playerRadius=12;for(let i=0;i<30;i++){stars.push({x:random(0,width),y:random(0,height),s:random(0.5,2)})}textSize(16);noStroke()}function draw(){background(0);for(let i=0;i<stars.length;i++){let st=stars[i];st.y+=st.s;if(st.y>height){st.y=0;st.x=random(0,width)}fill(255);ellipse(st.x,st.y,2,2)}if(!gameOver){if(keyIsDown(LEFT_ARROW)){playerX-=playerSpeed}if(keyIsDown(RIGHT_ARROW)){playerX+=playerSpeed}playerX=constrain(playerX,playerRadius,width-playerRadius);if(keyIsDown(32) && frameCount-lastShotFrame>=shotCooldown){bullets.push({x:playerX,y:playerY-10,r:4});lastShotFrame=frameCount}if(frameCount%60===0){enemies.push({x:random(12,width-12),y:-12,r:12})}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y-=8;if(b.y<-b.r){bullets.splice(i,1)}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];e.y+=2;if(e.y>height+e.r){enemies.splice(i,1);continue}if(dist(e.x,e.y,playerX,playerY)<=e.r+playerRadius){gameOver=true}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];if(dist(e.x,e.y,b.x,b.y)<=e.r+b.r){for(let k=0;k<5;k++){let angle=random(0,TWO_PI);let speed=random(1,3);particles.push({x:e.x,y:e.y,vx:cos(angle)*speed,vy:sin(angle)*speed,r:3,life:20})}enemies.splice(i,1);bullets.splice(j,1);score++;break}}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life--;if(p.life<=0){particles.splice(i,1)}}}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];fill(255,255,0);ellipse(b.x,b.y,b.r*2,b.r*2)}for(let i=0;i<enemies.length;i++){let e=enemies[i];fill(255,0,0);ellipse(e.x,e.y,e.r*2,e.r*2)}for(let i=0;i<particles.length;i++){let p=particles[i];let alpha=map(p.life,0,20,0,255);fill(255,150,0,alpha);ellipse(p.x,p.y,p.r*2,p.r*2)}fill(0,150,255);triangle(playerX,playerY-12,playerX-12,playerY+12,playerX+12,playerY+12);fill(255);textAlign(LEFT,TOP);textSize(16);text('Score: '+score,10,10);if(gameOver){fill(255);textAlign(CENTER,CENTER);textSize(32);text('GAME OVER',width/2,height/2)}}
