let player,bullets,enemies,particles,stars,score,gameOver;
function setup(){createCanvas(400,600);player={x:width/2,y:height-40,r:16,speed:5};bullets=[];enemies=[];particles=[];stars=[];score=0;gameOver=false;for(let i=0;i<30;i++){stars.push({x:random(width),y:random(height),sz:random(1,3),speed:random(0.5,2)})}textFont('monospace');}
function draw(){background(0);for(let i=stars.length-1;i>=0;i--){let s=stars[i];s.y+=s.speed;if(s.y>height){s.y=0;s.x=random(width)}noStroke();fill(255);ellipse(s.x,s.y,s.sz,s.sz)}if(!gameOver){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed}player.x=constrain(player.x,player.r,width-player.r)}noStroke();fill(0,255,255);triangle(player.x,player.y-player.r,player.x-player.r,player.y+player.r,player.x+player.r,player.y+player.r);for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y-=b.speed;if(b.y<-b.r){bullets.splice(i,1);continue}noStroke();fill(255,255,0);ellipse(b.x,b.y,b.r*2,b.r*2)}if(frameCount%60===0 && !gameOver){enemies.push({x:random(12,width-12),y:-12,r:12,spd:2})}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];if(!gameOver){e.y+=e.spd}if(e.y>height+e.r){enemies.splice(i,1);continue}noStroke();fill(255,0,0);ellipse(e.x,e.y,e.r*2,e.r*2);let hit=false;for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];let dx=e.x-b.x;let dy=e.y-b.y;let dist2=dx*dx+dy*dy;if(dist2< (e.r+b.r)*(e.r+b.r) ){enemies.splice(i,1);bullets.splice(j,1);for(let k=0;k<5;k++){particles.push({x:e.x,y:e.y,r:3,life:20,vx:random(-2,2),vy:random(-2,2)})}score++;hit=true;break}}if(hit)continue;let dxp=e.x-player.x;let dyp=e.y-player.y;let pd2=dxp*dxp+dyp*dyp;if(pd2<(e.r+player.r)*(e.r+player.r)){gameOver=true}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life--;if(p.life<=0){particles.splice(i,1);continue}noStroke();fill(255,150,0,200*(p.life/20));ellipse(p.x,p.y,p.r*2*((p.life)/20),p.r*2*((p.life)/20))}fill(255);textSize(16);textAlign(LEFT,TOP);text('Score: '+score,10,10);if(gameOver){textAlign(CENTER,CENTER);textSize(32);fill(255,0,0);text('GAME OVER',width/2,height/2)}}
function keyPressed(){if(keyCode===32 && !gameOver){bullets.push({x:player.x-6,y:player.y-player.r,r:4,speed:8});bullets.push({x:player.x+6,y:player.y-player.r,r:4,speed:8})}}
